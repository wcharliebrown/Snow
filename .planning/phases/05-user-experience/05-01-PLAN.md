---
phase: 05-user-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_user_experience.php
  - tests/test_all.php
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
  - EXT-04

must_haves:
  truths:
    - "Running `php tests/test_user_experience.php` exits without PHP fatals"
    - "All DATA-02, DATA-03, DATA-04, EXT-04 stubs are present and initially failing (not missing)"
    - "`php tests/test_all.php` includes and runs the Phase 5 test file"
  artifacts:
    - path: "tests/test_user_experience.php"
      provides: "Wave-0 test stubs for all Phase 5 requirements"
      exports: ["DATA-02 schema check", "DATA-03 unit test", "DATA-04 unit tests", "EXT-04 integration tests"]
    - path: "tests/test_all.php"
      provides: "Full suite runner including Phase 5"
      contains: "require_once __DIR__ . '/test_user_experience.php'"
  key_links:
    - from: "tests/test_all.php"
      to: "tests/test_user_experience.php"
      via: "require_once at end of file before summary()"
      pattern: "require_once.*test_user_experience"
---

<objective>
Create Wave-0 test stubs for Phase 5. These tests fail until implementation plans (05-02 through 05-04) are executed — that is intentional. They define the verification contract for DATA-02, DATA-03, DATA-04, and EXT-04.

Purpose: Nyquist compliance — every implementation task has an automated test command that runs in under 10 seconds.
Output: tests/test_user_experience.php with 10 stub tests; tests/test_all.php updated to include it.
</objective>

<execution_context>
@/home/cb/.claude/get-shit-done/workflows/execute-plan.md
@/home/cb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-experience/05-CONTEXT.md
@.planning/phases/05-user-experience/05-RESEARCH.md

<interfaces>
<!-- Existing test runner pattern — match exactly -->
<!-- From tests/test_extensibility.php (standalone guard pattern): -->
```php
if (!isset($t)) {
    require_once __DIR__ . '/SnowTestRunner.php';
    require_once __DIR__ . '/bootstrap.php';
    $t = new SnowTestRunner();
    $standaloneRun = true;
} else {
    $standaloneRun = false;
}
// ... describe blocks ...
if (isset($standaloneRun) && $standaloneRun) {
    $t->summary();
}
```

<!-- From tests/test_all.php (inclusion point): -->
<!-- Line 1677: require_once __DIR__ . '/test_data_integrity.php'; -->
<!-- Line 1680: require_once __DIR__ . '/test_extensibility.php'; -->
<!-- Line 1683 (end): $t->summary(); -->
<!-- Insert Phase 5 require_once BEFORE $t->summary() -->

<!-- DB helper for schema checks (from test_extensibility.php pattern): -->
```php
$col = dbGetRow(
    "SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS
     WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = ? AND COLUMN_NAME = ?",
    ['custom_table_fields', 'col_width']
);
$t->assertNotNull($col, 'custom_table_fields must have col_width column');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_user_experience.php with 10 Wave-0 stubs</name>
  <files>tests/test_user_experience.php</files>
  <action>
Create `/home/cb/Snow/tests/test_user_experience.php` following the exact pattern of `tests/test_extensibility.php`.

File header comment: "Phase 5: User Experience — test stubs (DATA-02, DATA-03, DATA-04, EXT-04)"
Standalone guard: same isset($t) pattern as test_extensibility.php.

Write these describe/it blocks covering all 10 stubs from 05-VALIDATION.md:

**DATA-02 (schema):**
```
describe('DATA-02: Edit Form Layout — col_width Column')
  it('custom_table_fields has col_width column after migration')
    → INFORMATION_SCHEMA check on custom_table_fields for column 'col_width'
    → assertNotNull — stub will fail until 05-02 migration runs
```

**DATA-03 (unit):**
```
describe('DATA-03: Edit Form col_width Rendering')
  it('col_width full resolves to col-12 Bootstrap class')
    → Pure PHP logic: $colClass = ($f['col_width'] ?? 'half') === 'full' ? 'col-12' : 'col-md-6'
    → assertEqual('col-12', value when col_width='full')
    → assertEqual('col-md-6', value when col_width='half')
    → assertEqual('col-md-6', value when col_width=null — default fallback)
    → This test passes immediately (pure logic, no DB), so it is green from Wave 0
```

**DATA-04 (unit — 4 tests in same describe block):**
```
describe('DATA-04: Search, Sort, Filter')
  it('WHERE builder produces LIKE clause for simple search q param')
    → Stub: assert false with message 'implement search WHERE builder in admin-custom-table.php'

  it('WHERE builder produces per-field LIKE for adv[] params')
    → Stub: assert false with message 'implement advanced search WHERE builder'

  it('sort field allowlist rejects unknown field names')
    → Pure logic test (can pass immediately):
      $allowedFields = ['name', 'email', 'status'];
      $sort = in_array('injected_col', $allowedFields, true) ? 'injected_col' : 'id';
      assertEqual('id', $sort) — passes immediately

  it('sort direction defaults to DESC when dir param is invalid')
    → Pure logic test (can pass immediately):
      $dir = strtolower('invalid') === 'asc' ? 'ASC' : 'DESC';
      assertEqual('DESC', $dir) — passes immediately
```

**EXT-04 (schema + integration — 4 tests):**
```
describe('EXT-04: Scheduled Row Lifecycle')
  it('a provisioned custom table has activate_at column after migration')
    → INFORMATION_SCHEMA check on a test table (use 'custom_tables' meta-table won't work;
      use INFORMATION_SCHEMA to check any registered active custom table, or skip with
      a $t->skip() if no custom tables exist yet)
    → Stub: assert false with message 'run 05-02 migration and migrateExistingCustomTables()'

  it('processScheduledActions() activates rows past their activate_at date')
    → Stub: assert false with message 'implement processScheduledActions() in admin-custom-table.php'

  it('processScheduledActions() deactivates rows past their deactivate_at date')
    → Stub: assert false with message 'implement processScheduledActions()'

  it('processScheduledActions() deletes rows past their delete_at date')
    → Stub: assert false with message 'implement processScheduledActions()'
```

For stub assertions that should FAIL until implementation: use `$t->assertTrue(false, 'message')` — this produces a clear failure message, not a PHP fatal.

End of file: standalone summary guard `if (isset($standaloneRun) && $standaloneRun) { $t->summary(); }`
  </action>
  <verify>
    <automated>php /home/cb/Snow/tests/test_user_experience.php</automated>
  </verify>
  <done>File runs without PHP fatals. Pure-logic tests (DATA-03 col_width rendering, DATA-04 allowlist/direction defaults) pass. Stub tests for DB-dependent behaviors fail with descriptive assertion messages (not PHP errors). Output shows describe/it results.</done>
</task>

<task type="auto">
  <name>Task 2: Register test_user_experience.php in test_all.php</name>
  <files>tests/test_all.php</files>
  <action>
In `/home/cb/Snow/tests/test_all.php`, add after line 1680 (the `require_once test_extensibility.php` line) and before the `$t->summary()` call:

```php
// Phase 5: User Experience tests
require_once __DIR__ . '/test_user_experience.php';
```

This must appear before the final `$t->summary();` line (currently the last line of the file). Confirm by reading the end of the file before editing.
  </action>
  <verify>
    <automated>php /home/cb/Snow/tests/test_all.php</automated>
  </verify>
  <done>Full suite runs without PHP fatals. Phase 5 tests appear in output. The suite may show failures (stub tests) — that is expected and correct at Wave 0.</done>
</task>

</tasks>

<verification>
- `php /home/cb/Snow/tests/test_user_experience.php` — exits 0, no PHP fatals, stub failures are assertion failures only
- `php /home/cb/Snow/tests/test_all.php` — full suite runs, Phase 5 tests appear in output
- Pure-logic tests (DATA-03 col_width, DATA-04 allowlist/direction) are green immediately
- Stub tests produce descriptive failure messages, not PHP errors
</verification>

<success_criteria>
- tests/test_user_experience.php exists with 10 tests covering DATA-02, DATA-03, DATA-04, EXT-04
- tests/test_all.php includes test_user_experience.php before summary()
- Running either file exits without PHP fatals
- Pure-logic tests pass; DB-dependent stubs fail with messages (expected)
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience/05-01-SUMMARY.md`
</output>
