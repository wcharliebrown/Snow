---
phase: 04-extensibility
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_extensibility.php
  - tests/test_all.php
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03, SEC-05]

must_haves:
  truths:
    - "php tests/test_all.php runs without fatal errors and includes extensibility tests"
    - "All EXT-01 hook tests are present (columns exist, pre_edit fires, post_edit fires, failure is non-blocking)"
    - "All SEC-05 OTP tests are present (schema exists, expiry respected, lockout at 3 attempts, session not set before OTP)"
    - "EXT-03 sendEmailTemplate token substitution test is present"
    - "EXT-02 custom_script include pattern test is present"
  artifacts:
    - path: "tests/test_extensibility.php"
      provides: "Test stubs for EXT-01, SEC-05, EXT-02, EXT-03"
      exports: [test stubs — all pass or skip until implementation lands]
    - path: "tests/test_all.php"
      provides: "Updated to require test_extensibility.php"
      contains: "require_once.*test_extensibility.php"
  key_links:
    - from: "tests/test_all.php"
      to: "tests/test_extensibility.php"
      via: "require_once at end of file"
      pattern: "require_once.*test_extensibility"
---

<objective>
Create tests/test_extensibility.php containing test stubs for all Phase 4 requirements, and add a require_once for this file at the end of tests/test_all.php.

Purpose: Nyquist compliance — all subsequent Phase 4 tasks need test coverage before implementation begins. These stubs will fail (or be marked pending) until the implementation plans land; they become green during execution of plans 02-05.

Output: tests/test_extensibility.php (new), tests/test_all.php (one line appended).
</objective>

<execution_context>
@/home/cb/.claude/get-shit-done/workflows/execute-plan.md
@/home/cb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extensibility/04-CONTEXT.md
@.planning/phases/04-extensibility/04-VALIDATION.md
</context>

<interfaces>
<!-- Existing test framework — executor must match this API exactly. -->

From tests/SnowTestRunner.php (observed pattern in test_all.php and test_data_integrity.php):
```php
$t->describe('Suite Name', function (SnowTestRunner $t) {
    $t->it('behavior description', function (SnowTestRunner $t) {
        $t->assertTrue(expr, 'message');
        $t->assertEqual($expected, $actual);
        $t->assertIsArray($value);
        $t->assertGreaterThan($expected, $actual, 'message');
        $t->assertContains('needle', $haystack);
    });
});
```

From tests/test_all.php (append pattern):
```php
// Last line of file:
require_once __DIR__ . '/test_data_integrity.php';
// New last line will be:
require_once __DIR__ . '/test_extensibility.php';
```

From tests/bootstrap.php: Sets up DB connection, loads all functions — tests can call dbGetRow(), dbGetRows() directly.

From tests/test_data_integrity.php (stub pattern for unimplemented features):
Tests that depend on schema use dbGetRow/INFORMATION_SCHEMA checks; if the column is absent the
test fails with a descriptive message rather than a PHP fatal — which is correct Wave-0 behavior.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/test_extensibility.php with all Phase 4 stubs</name>
  <files>tests/test_extensibility.php</files>
  <action>
Create a new file tests/test_extensibility.php using the SnowTestRunner API. Include four describe blocks — one per requirement. Each block contains the specific test cases listed below. Tests that check schema existence will pass once the migration runs (plan 04-02); tests that check behaviour will pass once the implementation plans run (04-03, 04-04). All tests must be structured so they fail gracefully (assertion failure message, not PHP fatal) when the feature is not yet present.

EXT-01 describe('EXT-01: Table Hooks', ...) — tests:
1. 'custom_tables has pre_edit_php_filename column' — use dbGetRow on INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='custom_tables' AND COLUMN_NAME='pre_edit_php_filename'; assertTrue column exists
2. 'custom_tables has post_edit_php_filename column' — same pattern for post_edit_php_filename
3. 'pre_edit hook file is included when it exists and tableDef has filename set' — simulate by: create a temp hook file in sys_get_temp_dir() that writes to a tempfile on include; set up $tableDef with pre_edit_php_filename pointing to it; call the hook logic inline (replicate the if($hookFile){include $hookPath;} block); assert the tempfile was written
4. 'hook failure does not throw — catch(Throwable) absorbs fatal' — create a temp "hook" file containing invalid PHP (<?php throw new \Exception("hook error");); wrap the include in try/catch(Throwable $e); assertTrue no exception propagates and logError was not fatal

SEC-05 describe('SEC-05: Email OTP 2FA', ...) — tests:
1. 'login_otp table exists' — INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME='login_otp'; assertTrue count > 0
2. 'login_otp has required columns: user_id, code, expires_at, attempts' — check all four via INFORMATION_SCHEMA.COLUMNS
3. 'users table has require_2fa column' — INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='users' AND COLUMN_NAME='require_2fa'
4. 'expired OTP code is not accepted' — insert a login_otp row with expires_at = NOW() - INTERVAL 1 MINUTE; attempt to SELECT WHERE expires_at > NOW(); assert no row returned; clean up
5. 'OTP with attempts >= 3 is not accepted' — insert a login_otp row with attempts=3; SELECT WHERE attempts < 3; assert no row returned; clean up
6. 'otp_pending_user_id is distinct from user_id in session logic' — assert that 'otp_pending_user_id' !== 'user_id' (string comparison — confirms the keys are separate; documents the invariant)

EXT-03 describe('EXT-03: Email Templates', ...) — tests:
1. 'sendEmailTemplate renders {{variable}} substitutions' — insert a test email_templates row with name='__test_ext03__', subject='Hello {{name}}', body='Code: {{code}}'; call processTokens($template['body'], ['name'=>'World', 'code'=>'123456']); assert result contains 'Code: 123456' (not the raw token); clean up by deleting the row
2. 'email_templates table has login_otp template seeded by migration' — dbGetRow("SELECT id FROM email_templates WHERE name = 'login_otp'"); assertTrue row exists

EXT-02 describe('EXT-02: Custom Pages', ...) — tests:
1. 'pages table has content and custom_script columns' — INFORMATION_SCHEMA.COLUMNS for TABLE_NAME='pages'; check both columns exist
2. 'custom_script include pattern: file is included when script column is set' — replicate the pages.php pattern: create a temp script file, set $page['custom_script'] to its basename, set SNOW_FUNCTIONS constant if not defined to sys_get_temp_dir(), call the inline include logic, assert file was included (write sentinel to tempfile)

Note: Use processTokens() from functions/template.php — it is already loaded by bootstrap.php.
Note: Define SNOW_FUNCTIONS as sys_get_temp_dir() only if not already defined (use defined() check).
Note: Clean up all inserted DB rows in test closures using try/finally or explicit cleanup at end of each test.
  </action>
  <verify>
    <automated>php tests/test_extensibility.php 2>&1 | tail -5</automated>
  </verify>
  <done>File exists and php tests/test_extensibility.php runs — some tests will fail (schema not yet present) but no PHP fatals; the SnowTestRunner summary line is printed.</done>
</task>

<task type="auto">
  <name>Task 2: Append require_once to tests/test_all.php</name>
  <files>tests/test_all.php</files>
  <action>
Read tests/test_all.php. The last line currently reads:
  require_once __DIR__ . '/test_data_integrity.php';

Append one line after it:
  require_once __DIR__ . '/test_extensibility.php';

Do not change any other content. Preserve all existing tests.
  </action>
  <verify>
    <automated>php tests/test_all.php 2>&1 | tail -10</automated>
  </verify>
  <done>php tests/test_all.php runs to completion (with some failures for unimplemented features — expected at Wave 0), no PHP fatals, extensibility suite is listed in output.</done>
</task>

</tasks>

<verification>
php tests/test_all.php runs without PHP fatal errors.
test_extensibility.php is included and its describe blocks appear in output.
All 12 test stubs are present and accounted for in the output (pass or fail — not missing).
</verification>

<success_criteria>
- tests/test_extensibility.php exists with 4 describe blocks covering EXT-01, SEC-05, EXT-03, EXT-02
- tests/test_all.php ends with require_once for test_extensibility.php
- php tests/test_all.php runs without fatal errors
- All pre-existing tests still pass (no regressions introduced)
</success_criteria>

<output>
After completion, create .planning/phases/04-extensibility/04-01-SUMMARY.md
</output>
