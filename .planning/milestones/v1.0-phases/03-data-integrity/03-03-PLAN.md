---
phase: 03-data-integrity
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - functions/admin-custom-table.php
autonomous: true
requirements:
  - VER-01

must_haves:
  truths:
    - "Every successful edit of a custom table row inserts one new row into row_versions before overwriting the live row"
    - "The inserted row_snapshot JSON contains the exact before-image of all columns in the row"
    - "Version capture does NOT fire when the ACL check fails or field validation fails"
  artifacts:
    - path: "functions/admin-custom-table.php"
      provides: "Version capture logic in the edit POST handler"
      contains: "row_versions"
  key_links:
    - from: "functions/admin-custom-table.php edit POST branch"
      to: "row_versions table"
      via: "dbInsert('row_versions', ...) inside if (!$hasError) block, immediately before dbUpdate()"
      pattern: "dbInsert.*row_versions"
---

<objective>
Add row version capture to the edit POST handler in admin-custom-table.php so every successful row edit is recorded with before-state and author.

Purpose: Implements VER-01 — the system records which user modified each row and what changed.

Output: Every successful custom table edit inserts a row into row_versions with the full JSON before-image, changed_by, and changed_at before the dbUpdate() runs.
</objective>

<execution_context>
@/home/cb/.claude/get-shit-done/workflows/execute-plan.md
@/home/cb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-integrity/03-CONTEXT.md
@.planning/phases/03-data-integrity/03-RESEARCH.md

<interfaces>
<!-- From functions/admin-custom-table.php — the edit POST branch (lines 97–141): -->

} elseif ($postAction === 'edit' && $recordId) {
    // Row-level edit permission check
    $existingForAcl = dbGetRow("SELECT * FROM `{$tableName}` WHERE id = ?", [$recordId]);
    if (!$existingForAcl || !canEditRow($existingForAcl)) {
        http_response_code(403);
        $error = 'You do not have permission to edit this record.';
        $hasError = true;
    }

    // ... field validation loop sets $hasError = true on failure ...

    if (!$hasError) {
        // ... builds $rowData ...
        // <<< VERSION CAPTURE GOES HERE, immediately before dbUpdate() >>>
        dbUpdate($tableName, $rowData, 'id = ?', [$recordId]);
        header('Location: /' . $page['path'] . '?msg=updated');
        exit;
    }
    $action = 'edit';
}

<!-- DB helpers available (functions/database.php): -->
dbGetRow(string $sql, array $params = []): array|false
dbInsert(string $table, array $data): int   // returns inserted id
dbUpdate(string $table, array $data, string $where, array $params): void
getCurrentUser(): array  // returns ['id' => int, 'username' => string, ...]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert version capture block inside the if (!$hasError) branch before dbUpdate()</name>
  <files>functions/admin-custom-table.php</files>
  <behavior>
    - When edit POST succeeds (ACL passes, validation passes), a new row_versions record is inserted with: table_name=$tableName, row_id=$recordId, changed_by=getCurrentUser()['id'], changed_at=date('Y-m-d H:i:s'), row_snapshot=json_encode of the full existing row
    - Version capture uses $existingForAcl (already fetched at line 99 for ACL check) — no second SELECT needed
    - Capture happens ONLY inside if (!$hasError) block — not when ACL fails, not when validation fails
    - If $existingForAcl is false (row does not exist), skip capture — dbUpdate will fail anyway
    - changed_by is $currentUser['id'] ?? null (nullable FK)
  </behavior>
  <action>
Read functions/admin-custom-table.php to see the exact current content of the if (!$hasError) block (around lines 127-141).

Find the line:
```php
            dbUpdate($tableName, $rowData, 'id = ?', [$recordId]);
```

Insert the version capture block immediately BEFORE that line, inside the `if (!$hasError)` block:

```php
            // VER-01: Capture row state before overwrite (before-image for version history)
            if ($existingForAcl) {
                $currentUser = getCurrentUser();
                dbInsert('row_versions', [
                    'table_name'   => $tableName,
                    'row_id'       => $recordId,
                    'changed_by'   => $currentUser['id'] ?? null,
                    'changed_at'   => date('Y-m-d H:i:s'),
                    'row_snapshot' => json_encode($existingForAcl),
                ]);
            }
            // end VER-01
```

Note: $existingForAcl is already set at line 99 (`$existingForAcl = dbGetRow(...)`) — reuse it, do not add a second SELECT.

After inserting, verify the file is syntactically valid with `php -l`.
  </action>
  <verify>
    <automated>cd /home/cb/Snow && php -l functions/admin-custom-table.php && php tests/test_all.php 2>&1 | tail -15</automated>
  </verify>
  <done>
    - functions/admin-custom-table.php has version capture block inside if (!$hasError), immediately before dbUpdate()
    - php -l reports no syntax errors
    - php tests/test_all.php passes with no new failures
    - The version capture code uses $existingForAcl (already fetched), not a new SELECT
    - VER-01 capture is guarded: only runs when $existingForAcl is truthy AND $hasError is false
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Version capture in admin-custom-table.php POST handler. Every successful custom table row edit now inserts a before-image into row_versions.
  </what-built>
  <how-to-verify>
    1. Open the Snow admin and navigate to any custom table (e.g. /admin/data/your_table)
    2. Click Edit on any row — note the current field values
    3. Change one field value and click Save
    4. Verify the redirect back to the list occurs with "Record updated." message
    5. In a MySQL client or via docker exec, run:
       SELECT * FROM row_versions ORDER BY id DESC LIMIT 1;
    6. Confirm: table_name matches the table you edited, row_id matches the row, row_snapshot JSON contains the BEFORE values (the values you saw before editing), changed_by matches your user id, changed_at is recent
    7. Edit the same row again — verify row_versions now has 2 entries for that row_id
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Manual: Edit a custom table row twice. Verify row_versions has two entries for that row with correct before-images.
Automated: php tests/test_all.php — no new failures.
</verification>

<success_criteria>
Editing a custom table row inserts one row_versions entry per successful edit with correct table_name, row_id, changed_by, changed_at, and row_snapshot JSON containing the pre-edit values. ACL failures and validation failures produce no row_versions entries.
</success_criteria>

<output>
After completion, create .planning/phases/03-data-integrity/03-03-SUMMARY.md
</output>
