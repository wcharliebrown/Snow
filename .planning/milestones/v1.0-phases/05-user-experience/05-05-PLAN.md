---
phase: 05-user-experience
plan: "05"
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
  - EXT-04

must_haves:
  truths:
    - "All Phase 5 automated tests pass (php tests/test_all.php exits green)"
    - "Search bar appears on a custom table list view with Submit button and Advanced toggle"
    - "Column headers are clickable sort links with arrow indicators"
    - "Search term and sort state are preserved together (search then sort keeps the search)"
    - "Advanced panel expands and accepts per-field inputs"
    - "col_width='full' field renders as full-width in the edit form"
    - "Schedule inputs appear in add/edit forms"
    - "Loading a list view triggers processScheduledActions() for that table"
  artifacts:
    - path: "functions/admin-custom-table.php"
      provides: "All Phase 5 UX features"
      contains: "processScheduledActions"
    - path: "functions/admin-tables.php"
      provides: "col_width UI in fields view"
      contains: "col_width"
  key_links:
    - from: "human tester"
      to: "browser"
      via: "manual verification steps below"
      pattern: "N/A"
---

<objective>
Human verification checkpoint for all Phase 5 features. Automated tests cover logic; this checkpoint confirms the browser-rendered UI behaves as specified.

Purpose: Catch rendering, state-preservation, and interactive behavior issues that automated PHP tests cannot detect.
Output: Confirmed or issues filed for gap-closure.
</objective>

<execution_context>
@/home/cb/.claude/get-shit-done/workflows/execute-plan.md
@/home/cb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-experience/05-CONTEXT.md
@.planning/phases/05-user-experience/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
Run the full test suite and confirm all tests pass:

```bash
php /home/cb/Snow/tests/test_all.php
```

All tests must be green before proceeding to the human verify checkpoint. If any tests fail, fix them first and do not advance to the checkpoint.
  </action>
  <verify>
    <automated>php /home/cb/Snow/tests/test_all.php</automated>
  </verify>
  <done>All tests pass. No failures in Phase 1-5 test suites.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 5 UX features in browser</name>
  <files></files>
  <action>
Verify Phase 5 User Experience features in the browser. Follow the how-to-verify steps below. Ensure Docker is running first: `cd /home/cb/Snow && docker-compose up -d`. Access at http://localhost.

What was built:
- DATA-02: col_width dropdown in fields management UI (admin-tables.php fields view)
- DATA-03: Edit form field width driven by col_width (half = col-md-6, full = col-12)
- DATA-04: Search bar, advanced search panel, sortable column headers on all list views
- EXT-04: Schedule inputs (activate_at, deactivate_at, delete_at) in add/edit forms; processScheduledActions() on list load
  </action>
  <verify>
    1. DATA-04 — Navigate to any custom table list view. Confirm: search bar appears with text input, "Search" button, status dropdown, and "Advanced" toggle. Type a term, click Search — results filter. Click a column header to sort — URL updates with ?sort=field&dir=asc and search term is preserved. Click header again — direction reverses with arrow indicator.

    2. DATA-04 Advanced — Click "Advanced" toggle — panel expands with per-field inputs. Fill a field, click Search — filters on that field. Sort while Advanced active — adv[] params survive sort click.

    3. DATA-02/DATA-03 — Navigate to /admin/tables, click Fields for a table. Confirm Width column in fields table. Add a field with Width=Full. Open add/edit form — full-width field spans full row; half-width fields are side-by-side.

    4. EXT-04 — Open add/edit form — "Scheduling" card section present with activate_at, deactivate_at, delete_at datetime inputs. Set activate_at to past datetime on inactive row, save, load list view — row status changes to active.
  </verify>
  <done>Human typed "approved" confirming all four feature areas work correctly in the browser.</done>
  <what-built>
    Phase 5 User Experience features:
    - DATA-02: col_width dropdown in fields management UI (admin-tables.php fields view)
    - DATA-03: Edit form field width driven by col_width (half = col-md-6, full = col-12)
    - DATA-04: Search bar, advanced search panel, sortable column headers on all list views
    - EXT-04: Schedule inputs (activate_at, deactivate_at, delete_at) in add/edit forms; processScheduledActions() on list load
  </what-built>
  <how-to-verify>
    Ensure Docker is running: `cd /home/cb/Snow && docker-compose up -d`
    Access the app at http://localhost (or configured port).

    **1. DATA-04 — Search and sort on list view**
    a. Navigate to any custom table list view (e.g. /admin/data/{tablename})
    b. Confirm: search bar appears above the table with a text input, "Search" button, status dropdown (All/Active/Inactive), and "Advanced" toggle button
    c. Confirm: column headers (ID, field labels, Status) are clickable links with no styling regression
    d. Type a search term and click Search — confirm results filter correctly
    e. Click a column header to sort — confirm URL updates with ?sort=field&dir=asc
    f. Confirm: the search term from step (d) is still visible in the search box and results still filtered after the sort click
    g. Click the column header again — confirm direction reverses and arrow indicator appears on sorted column
    h. Set status dropdown to "Active" and click Search — confirm only active rows appear

    **2. DATA-04 — Advanced search panel**
    a. Click the "Advanced" button — confirm the per-field search panel expands (Bootstrap collapse)
    b. Populate one per-field input and click Search — confirm results filter on that field value
    c. Confirm simple search box is not active when Advanced panel is used (mutually exclusive)
    d. Sort while Advanced panel is active — confirm adv[] params survive the sort click (panel stays open with values)

    **3. DATA-02/DATA-03 — col_width in fields management and edit form**
    a. Navigate to /admin/tables, click Fields for any custom table with at least one field
    b. Confirm: fields table shows a "Width" column showing 'half' or 'full' for each field
    c. In the Add Field form, confirm a Width dropdown (Half / Full) is present
    d. Add a test field with Width = Full
    e. Navigate to the data list view for that table, click Add or Edit a record
    f. Confirm the new Full-width field spans the full row (col-12) while Half fields are side-by-side (col-md-6)

    **4. EXT-04 — Schedule inputs and processScheduledActions**
    a. Open an add or edit form for a custom table row
    b. Confirm: a "Scheduling" card section appears with Activate At, Deactivate At, and Delete At datetime inputs
    c. Set Activate At to a datetime 1 minute in the past (e.g. 2026-03-17T00:00) on an inactive row and save
    d. Navigate to the list view for that table — confirm the row status has changed to "active" (processScheduledActions fired on page load)
    e. Optionally: set Delete At to a past datetime on a row and load the list — confirm the row is deleted
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g. "sort clears search", "Advanced panel not collapsing", "schedule inputs missing")</resume-signal>
</task>

</tasks>

<verification>
- All automated tests green before checkpoint
- Human confirmed all 4 feature areas work as specified in browser
</verification>

<success_criteria>
- Phase 5 automated test suite green
- Human-verified: search/sort/filter UI works end-to-end including state preservation
- Human-verified: col_width drives edit form layout
- Human-verified: schedule inputs present and processScheduledActions() fires on list load
- Phase 5 complete — all DATA-02, DATA-03, DATA-04, EXT-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience/05-05-SUMMARY.md`
</output>
